using UnityEngine;

// handle utilities and functions, not data, not really exist in the real game, belong to the class but not to the instance
public static class Utilities
{
    public enum GameState
    {
        Play,
        Pause,
        GameOver
    }
}
